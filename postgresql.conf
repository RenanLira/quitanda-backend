# ===================================
# POSTGRESQL CONFIGURATION
# ===================================
# Otimizado para servidor com 4GB RAM
# Ajuste conforme seus recursos

# ===================================
# CONEXÕES
# ===================================
max_connections = 100                   # Máximo de conexões simultâneas
superuser_reserved_connections = 3      # Conexões reservadas para superusuário

# ===================================
# MEMÓRIA
# ===================================
# Memória compartilhada (25% da RAM total)
shared_buffers = 1GB                    # DEV: 256MB, PROD: 1-4GB

# Memória para operações de ordenação/hash por conexão
work_mem = 16MB                         # DEV: 4MB, PROD: 16-32MB

# Memória para manutenção (VACUUM, CREATE INDEX)
maintenance_work_mem = 256MB            # DEV: 64MB, PROD: 256MB-1GB

# Cache efetivo total disponível (50-75% da RAM)
effective_cache_size = 3GB              # DEV: 512MB, PROD: 3-8GB

# ===================================
# CHECKPOINTS (Write-Ahead Log)
# ===================================
# Intervalo entre checkpoints
checkpoint_completion_target = 0.9      # Distribuir I/O ao longo do tempo

# Tamanho máximo do WAL
max_wal_size = 2GB                      # DEV: 1GB, PROD: 2-4GB
min_wal_size = 1GB                      # DEV: 512MB, PROD: 1-2GB

# ===================================
# QUERY PLANNER
# ===================================
# Custo estimado de operações
random_page_cost = 1.1                  # SSD: 1.1, HDD: 4.0
effective_io_concurrency = 200          # SSD: 200, HDD: 2

# ===================================
# PARALELISMO
# ===================================
max_worker_processes = 4                # Número de CPUs
max_parallel_workers_per_gather = 2     # Workers por query paralela
max_parallel_workers = 4                # Total de workers paralelos
max_parallel_maintenance_workers = 2    # Workers para VACUUM, CREATE INDEX

# ===================================
# LOGGING (ajuste conforme necessidade)
# ===================================
# Nível de log
log_min_duration_statement = 1000       # Log queries > 1 segundo (DEV: 500, PROD: 1000)
log_line_prefix = '%t [%p]: [%l-1] user=%u,db=%d,app=%a,client=%h '
log_checkpoints = on
log_connections = on
log_disconnections = on
log_lock_waits = on

# Estatísticas
shared_preload_libraries = 'pg_stat_statements'
pg_stat_statements.track = all

# ===================================
# AUTOVACUUM (Manutenção Automática)
# ===================================
autovacuum = on
autovacuum_max_workers = 3
autovacuum_naptime = 30s                # DEV: 60s, PROD: 30s

# ===================================
# CLIENTE
# ===================================
timezone = 'America/Sao_Paulo'
lc_messages = 'pt_BR.UTF-8'
lc_monetary = 'pt_BR.UTF-8'
lc_numeric = 'pt_BR.UTF-8'
lc_time = 'pt_BR.UTF-8'
default_text_search_config = 'pg_catalog.portuguese'

# ===================================
# NOTAS
# ===================================
# Depois de alterar este arquivo:
# 1. Reinicie o container: docker-compose restart postgres
# 2. Ou recarregue configs: docker exec postgres_db pg_ctl reload